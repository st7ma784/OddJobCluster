# This is a placeholder for the munge key
# In production, this should be a randomly generated key shared across all nodes
# Generate with: dd if=/dev/urandom bs=1 count=1024 > /etc/munge/munge.key
{{ ansible_date_time.epoch }}{{ inventory_hostname }}{{ ansible_machine_id }}
